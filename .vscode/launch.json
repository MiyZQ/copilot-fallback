{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "调试插件",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "编译 TypeScript"
    }
  ]
}